[package]
name = "divergence-arena"
version = "0.1.0"
edition = "2021"
description = "Guess 2/3 of the Average — Pinocchio BPF program"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "0.9"
pinocchio-log = { version = "0.3", features = ["macro"] }
pinocchio-pubkey = "0.3"
pinocchio-system = "0.4"
